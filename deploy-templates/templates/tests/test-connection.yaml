apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-rest.fullname" . }}-test-connection"
  labels:
    {{- include "backend-rest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-rest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
